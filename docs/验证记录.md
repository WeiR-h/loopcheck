# 当前验证结果：v0.3.0 · 2026-09-09

## 自动化测试

最终完整后端测试：`python -m unittest discover -s tests -v`。
结果：**Ran 14 tests in 140.097s — OK**。
覆盖预算持久化、模型参数、Strands 工具流程、真实 Chromium、文件守护、停止、版本冲突、导出及会话隔离。测试模型是明确的替身；下面的真实 API 记录单独列出。

## 真实模型

百炼北京 qwen3.7-flash：初次失败保留，改进结构化工具参数后，三个原创故障及一次后台回归共四次通过。
成功耗时 33.13 / 37.11 / 25.87 / 37.14 秒；独立检查分别 5/5、6/6、7/7、8/8。
总计 28 次请求，估算 ¥0.0167164。没有未返回用量的预留金额。见 `evidence/live-summary.json` 与对应报告。

这是有限示例的实际运行记录，不能转换成通用成功率或人工效率提升百分比。

## 跨平台与界面

Docker Linux 构建成功，镜像 `loopcheck:v0.3.0`；非 root 用户 loopcheck，健康检查 healthy。
在独立端口 8793 运行真实 Chromium：4 项检查执行，2 项正常通过、2 项种入故障按预期失败；模型请求为 0；下载的完整应用包含 LICENSE。临时容器验证后已停止。
实际界面完成桌面/手机检查、英文路线、键盘标签切换、采用、后台通知、截图对比和历史记录验证。详见 `UI-QA.md`、`体验审查.md`。

## 尚未验证

公网托管、公开视频链接、公开仓库和 Devpost 最终提交尚未完成。AgentCore 未部署。未进行外部用户效率实验。

---

以下为历史记录，保留失败及过程；旧状态不代表当前版本。

# 第一版验证记录

日期：2026-09-08。以下区分真实浏览器执行、离线模型协议测试和真实模型调用。

| 项目 | 结果 | 证据类型 |
| --- | --- | --- |
| 原创任务清单的同名误删、混排误删 | 确实复现失败 | 真实 Playwright Chromium |
| 原版本新增、勾选及刷新 | 通过 | 真实 Playwright Chromium |
| 修复副本、采用、恢复、保存要求、重新引入缺陷后复查 | 通过 | 真实 Strands 调度 + 测试专用脚本模型 + 真实浏览器；不是在线模型能力测试 |
| 版本隔离、旧补丁拒绝、预算原子预留与重启保留 | 通过 | 自动化测试 |
| 跨站写请求、跨会话访问、超大请求与异常 Host | 已拒绝 | FastAPI 测试客户端 |
| 桌面与手机页面、检查按钮、导航、证据导出、人工时间记录 | 通过 | 真实浏览器对本地服务 |
| 浏览器控制台异常、手机横向溢出 | 未发现 | 页面操作测试 |
| Qwen3.7 Flash 真实修复 | 尚未验证 | 2026-09-09 改为此模型，当前缺少百炼 API 密钥 |
| Docker 镜像及 Linux 内浏览器 | 未通过环境验证 | 镜像解包时磁盘耗尽并出现 I/O 错误；本地原生启动与浏览器检查正常 |
| 真人效率对照、公开评审部署、演示视频 | 未完成 | 第一版本地程序不等于比赛提交已完成 |

已运行 `python -m unittest discover -s tests -v`，10 项通过，耗时约 29 秒。UI 检查使用 `python tests/ui_smoke.py`，通过且没有模型请求。

Docker 构建使系统盘空间降至约 8 MB；已移除本次下载但运行器不使用的完整 Chromium 缓存，释放约 447 MB，保留已验证使用的 headless shell。第一次删除镜像被 Docker 内部文件系统只读状态拒绝。在确认没有运行容器后，Docker Desktop 已成功重启，随后 `loopcheck:v0.1` 失败镜像删除成功；未删除其他镜像或数据卷。重启后系统盘可用空间约 1.95 GB，本地服务健康检查通过。当前不继续重建镜像。安装脚本及 Dockerfile 均改为仅下载需要的 headless shell。

测试脚本模型只位于 `tests/test_v1.py`，通过依赖注入进入测试 Engine。产品没有测试模型开关，也没有预写修复补丁的展示模式。测试运行保存在临时测试目录，不能冒充用户的真实模型运行记录。

UI 截图位于本地 `.test-data/ui/`；产品每次运行的真实截图、输入、结果保存于 `data/app/artifacts/`。后者可能含用户导入内容，已忽略，不进入公开源码。

此前 Bedrock 连通尝试为 Luna 两次、Qwen 一次，均被服务拒绝。服务未返回计费 token 数，尚不能认定其费用为零。当前应用费用面板仅统计应用自身经预算控制器发出的模型请求，早期连接测试须另外对照 AWS 账单。

不宣称已测得普遍效率提升，不用机器等待时间代替用户实际操作时间。完整参赛演示必须补做获准的真实模型修复和免费评审访问验证。

## 2026-09-09 模型适配

默认模型改为 `qwen3.7-flash`，北京地域短上下文输入/输出价按每百万 token 0.2/0.8 元计；首版显式关闭深度思考。新增 `test_model_adapter.py` 已通过：拦截实际序列化的 HTTP 请求，核对模型名称、工具定义、思考开关及输出上限；确认请求前费用预留，以及返回用量后的 SQLite 持久化结算。使用 `httpx.MockTransport`，未发送供应商网络请求，不能视为在线成功记录。原有 10 项测试结果保留为 9 月 8 日验证记录，本次不冒充重新执行。

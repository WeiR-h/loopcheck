# LoopCheck 0.5 自动验证与交付状态

本轮目标是要求管理与可信覆盖判断。人工效率实验未进行，不宣称节省人工时间比例。

## 已执行验证

- 首批完整测试：29 项通过，184.749 秒。随后增加 Strands 字典参数兼容测试，5 项 0.5 专项测试通过，57.288 秒。最终完整结果以本文件后续追加记录为准。
- 首轮升级测试发现 SQLite 备份连接未关闭，导致 Windows 无法清理临时数据库；修复后重跑通过。测试使用独立目录，未删除用户原始数据。
- 12 个冻结改动案例全部符合预期：6 个注入故障、6 个正常改动，错误放行 0，误报 0。流程由开发者预先定义，不是模型规划成功率。见 `evidence/v05-benchmark.json` 与 `v05-frozen-cases.json`。
- 购物车真实浏览器验证：基线通过 → 保存不存在的优惠券要求后未完成 → 实现优惠券并注入总价回归后失败 → 修复源码后通过。见 `evidence/v05-cart-gate.json`。源码修改与确认由验证脚本执行，明确为自动化开发场景，不冒充真人测试或编码代理自主修复。

## 真实 Strands / Qwen 四目标评测

固定版本的 MDN shopping list 与 dialog 两个公开 CC0 样例。四个自然语言目标、参考业务预期和规划器源码摘要在各轮模型调用前保存。参考预期和验收脚本不传给模型。评测程序模拟确认以测试后续执行，不能代替人审业务正确性。

| 轮次 | 通过目标 | 观察到的问题 | 模型调用 | 已报告费用（元） |
|---|---:|---|---:|---:|
| 初始 | 0/4 | Strands 工具传入字典，原处理器错误当成模型对象 | 24 | 0.0133786 |
| 参数修复后 | 1/4 | 父节点包含 Delete 文字；遗漏断言值；有一次只输出 JSON 未保存草稿 | 25 | 0.0203026 |
| 参数与提示修复后 | 2/4 | 弹窗两目标通过；添加目标仍选错文字范围；删除目标触发 8 次调用上限 | 32 | 0.0268694 |
| 重放与失败纠错试验 | 0/4 | 要求拆分冗余、重试编号错误，四目标均保留为未完成；调用上限未放宽 | 40 | 0.0370764 |
| 精简步骤与短编号修复后 | 4/4 | 四个目标均有真实浏览器检查通过，完整业务断言经逐项审查 | 35 | 0.0236614 |

五轮合计 156 次调用、0.1212884 元已报告费用，无未知预留费用。每轮四目标全部保留，包括失败和未完成。见 `v05-planning-frozen*.json`、`v05-planning-results*.json`。这是对同一小样例集调试后的结果，不能当作独立泛化测试，也不能只宣传“弹窗 100%”。

**当前限制：最新四目标通过仅代表同一小样例集的调试结果，不代表陌生项目成功率。** 早期父容器文字范围错误、重复规划和编号错误已增加纠错机制；最多三次观察、两次试运行及每任务八次模型请求的上限保持不变。 用户必须检查原始目标、业务预期和执行步骤。错误步骤可经用户确认“重建检查”，保留业务预期并重新生成。模型没有生成检查、生成失败或触发预算/调用上限时，都不能产生本次验收通过。

## 独立交付清单

| 项目 | 本轮状态 |
|---|---|
| 本地 0.5 运行、升级备份、自动浏览器验证 | 已执行；最终复核记录见下 |
| 安装包、英文材料、核心证据视频 | 本轮生成并验证后在下方记录 |
| 0.5 源码与 Release | [已发布](https://github.com/WeiR-h/loopcheck/releases/tag/v0.5.0)；匿名下载与包内摘要核验通过 |
| 0.5 Linux CI 与容器验证 | [已通过](https://github.com/WeiR-h/loopcheck/actions/runs/34589055868)；35 测试、12 案例、购物车/MCP、容器 |
| AWS 免费评审入口 | 未完成；本轮未创建付费资源 |
| YouTube/Vimeo 公共视频 | 未发布；本地视频文件不等于公开视频 |
| Builder Center 文章、Devpost 提交 | 未完成 |

没有获奖保证，也没有将本地通过写成云端已部署或比赛已提交。

## 最终本地复核

- 完整 33 项测试通过，211.163 秒；覆盖重建检查、停用配额、过期草稿和重启中断，见 `evidence/v05-tests.json`。
- 实际 MCP stdio 验证完成：旧记录 `can_accept=false`、完整失败步骤与截图可读取、最新四条检查通过，见 `evidence/v05-mcp.json`。
- 已生成 63.64 秒英文字幕验证片段，实际购物车浏览器录制，明确披露脚本注入和剪辑；不是连续真人操作录像，见 `evidence/v05-video-check.json`。
- 界面实测：原有预算记录迁移后显示历史结果；重新检查通过；购物车在控件不存在时通过表单保存优惠券要求，明确显示未覆盖和未完成。此界面验证由开发代理操作，不视为用户研究。

- 最后补充隐藏断言保护后，9 项 0.5 专项测试全部通过（87.041 秒）；两个已生成弹窗目标在新保护下均可重跑通过。不存在的目标不能凭“应当隐藏”变成通过。
- 最终 12 个冻结案例再次全部符合预期；保留前一轮结果为 `v05-benchmark-attempt1.json`。
- 解压后的安装包通过模块编译与 2 项 HTTP/公开模式检查；本机 Docker 服务不可用，因此尚无 0.5 本机容器运行结论。

- 断线／重启界面复核通过：关闭本地服务后显示启动提示和重试入口，依赖服务的按钮禁用；重启后自动恢复，要求与未完成结果保留。见 `evidence/v05-ui-check.json`。

## 2026-09-11 规划可靠性续作

- 完整 35 项测试通过，186.754 秒，全部使用隔离数据目录。包括短编号与原编号兼容、混合编号重复拒绝、无断言拒绝、重试不放宽预期、草稿仍需用户确认。见 `evidence/v05-reliability-tests.json`。
- 最终 12 个冻结案例再次全部符合预期：6 个故障无错误放行、6 个正常改动无误报。前两份结果保留为 `v05-benchmark-attempt1.json` 与 `v05-benchmark-attempt2.json`。
- 最新模型规划的步骤经开发代理逐条核对：添加包含文本与输入清空断言；删除包含添加、删除动作及列表数量为零；弹窗包含可见、关闭和选项值检查。见 `evidence/v05-planning-review.json`。这不是独立人工评审。
- 新的执行步骤接口由服务端复制已确认业务文本；短编号只在本次规划中映射到稳定编号与版本。一次失败后允许根据实际页面线索修改定位或前置步骤，禁止删除断言、改变断言类型或预期值。未通过结果不会被改写为通过。
- 继续保留 Strands、Qwen3.7 Flash 和现有预算。本次两轮新增已报告模型费用 0.0607378 元；没有创建付费资源，也未进行人工效率实验。

## 公开发布复核（2026-09-11）

已核验提交 `dde7f9fec694252f08dde7c3cc2895c72ea5a9ee` 的 Linux CI：35 测试通过（55.541 秒），12 案例无误放行或误报，购物车与实际 MCP 交接通过。768 MiB 限制的容器检查通过，峰值 326.74 MiB，无 OOM，未调用付费模型。见 [CI](https://github.com/WeiR-h/loopcheck/actions/runs/34589055868) 与 `evidence/v05-publication.json`。

[0.5.0 Release](https://github.com/WeiR-h/loopcheck/releases/tag/v0.5.0) 在检查成功后生成，公开包已匿名下载并校验全部清单文件。公开包摘要为 `3cd4fe98d2d8254478046584ca2d25d151d048985e44d924655da80c80538c82`；与 Windows 本地压缩包的字节摘要可不同。下载包绑定上述通过验证的提交，后续发布状态文档不改写该版本。AWS 实例运行、公共演示、YouTube/Vimeo 和 Devpost 仍是独立未完成项。

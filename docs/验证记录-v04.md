# LoopCheck 0.4 verification

Checked on 2026-09-09. No human-efficiency percentage is claimed; the user deferred the timing study.

## Completed checks

- First gate: real Pocket Budget arithmetic, negative-input validation and reload persistence. All passed, injected arithmetic fault failed, restoration passed. Zero model calls.
- Automated suite: 25 tests passed in 92.667 seconds in the final isolated suite. A concurrent real MCP run also passed. Existing v0.3 behavior, model budgets, source/version protection, session isolation and new project tests were included.
- Frozen benchmark: 12 / 12 expected outcomes across Pocket Budget, TodoMVC React and LoopCheck frontend. Six injected faults detected, six normal modifications passed; zero false greens and zero false alarms in this set. Developer-defined (AI-assisted) contracts were frozen separately from mutations; no model saw the mutation answers.
- Live model: first proposal contained semantic/action errors and was retained. Page-element metadata and proposal trial execution were added. The subsequent live Qwen/Strands proposal completed and three browser flows passed after UI approval.
- Actual stdio MCP: three tools discovered; passed → failed → passed, with the coding agent applying an external source patch from returned evidence. The failed calculation expected 850.00 and observed 1550.00.

## Evidence boundaries

Original sample and deliberately seeded defects. The React fixture is pinned public third-party code. LoopCheck frontend testing uses the actual frontend and a local proxy to its running backend. Tests are not production usage or a market study. Screenshots and execution durations cannot demonstrate human time saved.

Intermediate completion-state, stale-followup and test-storage failures and their fixes are retained in the release check record.

The final suite/packaging/media checks are recorded in `docs/evidence/v04-release-checks.json`. A missing or failed item there must not be described as verified. Public source, public video and cloud deployment are pending account access and release verification.

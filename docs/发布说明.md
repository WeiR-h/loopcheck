# LoopCheck 0.4 本地交付与公开发布

本地入口：http://127.0.0.1:8791/ 。英文入口：http://127.0.0.1:8791/judge 。

源码包：`dist/loopcheck-v0.4.0-source.zip`；SHA256：`dist/SHA256SUMS.txt`。验证记录：`docs/验证记录-v04.md`。英文提交草稿：`docs/SUBMISSION.en.md`。架构图：`docs/architecture.md`。MCP 接入：`docs/MCP.md`。

2026-09-10：[公开源码](https://github.com/WeiR-h/loopcheck)已发布；[Linux CI](https://github.com/WeiR-h/loopcheck/actions/runs/34462275115)通过 25 项测试、12 个冻结改动案例和全新 Docker 构建验收。容器在 768 MiB 上限下峰值 324.27 MiB，无 OOM；此次 CI 未调用模型，不能代替真实 AWS 1 GB 实例负载验证。云端免费演示、YouTube/Vimeo 视频、Builder Center 文章和正式提交仍待完成。尚未新建付费 AWS 资源，AgentCore 未部署。

本机 Codex 已追加 LoopCheck MCP 配置，保留其他服务器；需要刷新 MCP 工具或重启客户端。当前会话的工具热加载未验证。

新版视频：`dist/LoopCheck-demo-en-v0.4.mp4`（127 秒、英文字幕、无旁白，真实片段与截图剪辑讲解）。字幕：`dist/LoopCheck-demo-en-v0.4.srt`。

## 已准备的部署配置

`compose.yaml` 设置 `APP_PUBLIC=true`，对外版本只接受随包样例，禁用连接用户目录、手工要求接口和 MCP，并拒绝旧版任意源码导入。本地使用 `python run.py`，保留真实目录接入。单应用进程，共享一个浏览器执行槽位。

在已登录的 AWS 账户中部署前核验实际费用，保持总预算 100 元以内（模型20元、托管最多80元），不自动升级实例。需要持久卷保存数据库和预算。`APP_ALLOWED_HOSTS` 填实际域名，使用 HTTPS 反向代理；默认 compose 仅绑定本机端口。

供应商密钥只能进入服务器环境变量或不公开的 .env，不能进入源码仓库、视频或报告。`data`、`.test-data` 和桥接凭据不可发布。发布后从未登录浏览器实际检查 /judge 和模型连接，并保持评审期可用。

## 尚需完成的发布动作

1. 已完成公开 GitHub 源码、MIT 许可和成功的 Linux CI；固定版本包随 Release 提供。
2. 在预算内部署、验证运行内存和费用，取得免费评审链接。
3. 按 `docs/DEMO_SCRIPT.en.md` 补录连续操作主片；现有证据讲解片明确标注了剪辑与截图。
4. 发布视频和作者审阅后的技术文章，填写真实链接、Builder 身份与作者信息。
5. 核对官方截止日期和全部字段后正式提交；保存回执。

人工效率实验按用户要求暂缓，不宣称节省人工验收时间比例。

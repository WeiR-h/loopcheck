# LoopCheck 0.5 core evidence walkthrough

The 63.64-second supplement uses real Chromium clips from `scripts/cart_gate_v05.py`, with explicit cuts and held frames. The script approves test requirements and injects source changes; this is not a continuous human session or autonomous coding-agent repair claim.

1. Baseline: quantity, price calculation, validation and persistence are checked in a clean browser.
2. Add the future SAVE10 business requirement before a coupon field exists. Show the JSON result: three passing checks, one uncovered requirement, overall incomplete. The browser clip shows the still unchanged app.
3. Implement the coupon and inject a unit-price regression. Old quantity-two expectation is 200.00, observed 300.00. Show the full evidence brief and related changed file; do not claim that a changed file alone proves root cause.
4. Restore the unit price, retain the coupon, and recheck all four requirements. Coupon quantity two gives 180.00.
5. Actual MCP transport evidence is separately available in `docs/evidence/v05-mcp.json`: historical approval false, failed reproduction retrieved, four latest checks passed.

For the final competition video, record the actual coding tool consuming evidence and changing source, include the LoopCheck confirmation screens, and disclose cuts. That continuous end-user workflow recording and public video upload remain pending; do not relabel this supplement as either.
